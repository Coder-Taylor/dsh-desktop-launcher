# Linux 平台层

包含 XDG 路径、systemd user service、shell 环境、desktop 文件、AppImage/tar.gz 打包及浏览器适配。

Linux 默认使用用户权限，不静默调用 `sudo`。

本平台层随项目自有代码按 [MIT License](../../LICENSE) 授权；DSH 与其依赖仍遵循上游许可，详见[授权说明](../../docs/LICENSING.md)。

