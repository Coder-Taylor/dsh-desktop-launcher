# Windows 平台层

包含 Windows 进程树、浏览器、路径、任务计划、快捷方式、安装包及备用 BAT 引导适配。

现有 `启动DeepSeek Harness.bat` 暂时保留在仓库根目录；GUI 完成并通过迁移验收后再放入 `legacy/windows/`。

本平台层随项目自有代码按 [MIT License](../../LICENSE) 授权；DSH 与其依赖仍遵循上游许可，详见[授权说明](../../docs/LICENSING.md)。

